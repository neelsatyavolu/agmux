/**
 * @deprecated Prefer `ChatTasksPanel` (right-side floating tasks).
 * Type re-export kept so stickyTodos / codexPlan / existing imports keep working.
 */

export type { TodoBarItem } from "./ChatTasksPanel";
